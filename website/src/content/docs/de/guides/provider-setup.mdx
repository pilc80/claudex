---
title: Anbieter-Einrichtungsanleitung
description: Schritt-fuer-Schritt-Anleitung zur Konfiguration jedes KI-Anbieters mit Claudex
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Diese Anleitung enthaelt detaillierte Einrichtungsanweisungen fuer alle von Claudex unterstuetzten Anbieter, einschliesslich API-Schluessel-Anbieter, OAuth-Abonnements, Cloud-Plattformen und lokale Modelle.

## API-Schluessel-Anbieter

### Anthropic

1. Auf [console.anthropic.com](https://console.anthropic.com/) registrieren
2. Zu **API Keys** navigieren und einen neuen Schluessel erstellen (Format: `sk-ant-*`)

```toml
[[profiles]]
name = "anthropic"
provider_type = "DirectAnthropic"
base_url = "https://api.anthropic.com"
api_key = "sk-ant-..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex-config profile test anthropic
```

### MiniMax

1. Auf [platform.minimaxi.com](https://platform.minimaxi.com/) registrieren
2. In der Konsole zu **API Keys** navigieren

```toml
[[profiles]]
name = "minimax"
provider_type = "DirectAnthropic"
base_url = "https://api.minimax.io/anthropic"
api_key = "..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex-config profile test minimax
```

### OpenRouter

1. Auf [openrouter.ai](https://openrouter.ai/) registrieren
2. API-Schluessel unter [openrouter.ai/keys](https://openrouter.ai/keys) abrufen

```toml
[[profiles]]
name = "openrouter"
provider_type = "OpenAICompatible"
base_url = "https://openrouter.ai/api/v1"
api_key = "sk-or-..."
default_model = "anthropic/claude-sonnet-4"
```

```bash
claudex-config profile test openrouter
```

<Aside type="tip">
  OpenRouter unterstuetzt Hunderte von Modellen verschiedener Anbieter. Das Praefix `anthropic/` fuer Claude-Modelle verwenden.
</Aside>

### Grok (xAI)

1. Auf [console.x.ai](https://console.x.ai/) registrieren
2. In der Konsole einen API-Schluessel erstellen

```toml
[[profiles]]
name = "grok"
provider_type = "OpenAICompatible"
base_url = "https://api.x.ai/v1"
api_key = "xai-..."
default_model = "grok-3-beta"
```

```bash
claudex-config profile test grok
```

### OpenAI

1. Auf [platform.openai.com](https://platform.openai.com/) registrieren
2. Einen API-Schluessel unter [platform.openai.com/api-keys](https://platform.openai.com/api-keys) erstellen

```toml
[[profiles]]
name = "chatgpt"
provider_type = "OpenAICompatible"
base_url = "https://api.openai.com/v1"
api_key = "sk-..."
default_model = "gpt-4o"
```

```bash
claudex-config profile test chatgpt
```

### DeepSeek

1. Auf [platform.deepseek.com](https://platform.deepseek.com/) registrieren
2. In der Konsole einen API-Schluessel erstellen

```toml
[[profiles]]
name = "deepseek"
provider_type = "OpenAICompatible"
base_url = "https://api.deepseek.com"
api_key = "sk-..."
default_model = "deepseek-chat"
```

```bash
claudex-config profile test deepseek
```

### Kimi / Moonshot

1. Auf [platform.moonshot.cn](https://platform.moonshot.cn/) registrieren
2. In der Konsole einen API-Schluessel erstellen

```toml
[[profiles]]
name = "kimi"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.ai/v1"
api_key = "sk-..."
default_model = "kimi-k2-0905-preview"
```

```bash
claudex-config profile test kimi
```

### GLM / Zhipu

1. Auf [open.bigmodel.cn](https://open.bigmodel.cn/) registrieren
2. In der Konsole einen API-Schluessel erstellen

```toml
[[profiles]]
name = "glm"
provider_type = "OpenAICompatible"
base_url = "https://api.z.ai/api/paas/v4"
api_key = "..."
default_model = "glm-4.6"
```

```bash
claudex-config profile test glm
```

### Groq

1. Auf [console.groq.com](https://console.groq.com/) registrieren
2. Einen API-Schluessel unter **Settings > API Keys** erstellen

```toml
[[profiles]]
name = "groq"
provider_type = "OpenAICompatible"
base_url = "https://api.groq.com/openai/v1"
api_key = "gsk_..."
default_model = "llama-3.3-70b-versatile"
```

```bash
claudex-config profile test groq
```

<Aside type="tip">
  Groq bietet extrem schnelle Inferenz fuer Open-Source-Modelle. Beliebte Modelle sind `llama-3.3-70b-versatile`, `mixtral-8x7b-32768` und `gemma2-9b-it`.
</Aside>

### Mistral AI

1. Auf [console.mistral.ai](https://console.mistral.ai/) registrieren
2. In der Konsole einen API-Schluessel erstellen

```toml
[[profiles]]
name = "mistral"
provider_type = "OpenAICompatible"
base_url = "https://api.mistral.ai/v1"
api_key = "..."
default_model = "mistral-large-latest"
```

```bash
claudex-config profile test mistral
```

### Together AI

1. Auf [api.together.ai](https://api.together.ai/) registrieren
2. Im Dashboard einen API-Schluessel erstellen

```toml
[[profiles]]
name = "together"
provider_type = "OpenAICompatible"
base_url = "https://api.together.xyz/v1"
api_key = "..."
default_model = "meta-llama/Llama-3.3-70B-Instruct-Turbo"
```

```bash
claudex-config profile test together
```

### Perplexity

1. Auf [perplexity.ai](https://www.perplexity.ai/) registrieren
2. Einen API-Schluessel unter [perplexity.ai/settings/api](https://www.perplexity.ai/settings/api) erstellen

```toml
[[profiles]]
name = "perplexity"
provider_type = "OpenAICompatible"
base_url = "https://api.perplexity.ai"
api_key = "pplx-..."
default_model = "sonar-pro"
```

```bash
claudex-config profile test perplexity
```

<Aside type="note">
  Perplexity-Modelle verfuegen ueber eingebaute Websuch-Funktionen. Nuetzlich fuer rechercheorientierte Aufgaben.
</Aside>

### Cerebras

1. Auf [cloud.cerebras.ai](https://cloud.cerebras.ai/) registrieren
2. Im Dashboard einen API-Schluessel erstellen

```toml
[[profiles]]
name = "cerebras"
provider_type = "OpenAICompatible"
base_url = "https://api.cerebras.ai/v1"
api_key = "..."
default_model = "llama-3.3-70b"
```

```bash
claudex-config profile test cerebras
```

## Cloud-Plattformen

### Azure OpenAI

1. Eine Azure OpenAI-Ressource im [Azure Portal](https://portal.azure.com/) erstellen
2. Ein Modell bereitstellen und den Ressourcennamen sowie den Bereitstellungsnamen notieren
3. Den API-Schluessel unter **Schluessel und Endpunkt** abrufen

```toml
[[profiles]]
name = "azure-openai"
provider_type = "OpenAICompatible"
base_url = "https://YOUR_RESOURCE.openai.azure.com/openai/deployments/YOUR_DEPLOYMENT"
api_key = "YOUR_AZURE_KEY"
default_model = "gpt-4o"

[profiles.query_params]
api-version = "2024-12-01-preview"
```

```bash
claudex-config profile test azure-openai
```

<Aside type="note">
  Azure OpenAI wird automatisch erkannt, wenn `base_url` den String `openai.azure.com` enthaelt. Die Authentifizierung verwendet den `api-key`-Header anstelle von `Authorization: Bearer`. Der `api-version`-Abfrageparameter ist erforderlich.
</Aside>

### Google Vertex AI

1. Die Vertex AI API in Ihrem GCP-Projekt aktivieren
2. Ein Access Token mit `gcloud auth print-access-token` generieren

```toml
[[profiles]]
name = "vertex-ai"
provider_type = "DirectAnthropic"
base_url = "https://us-east5-aiplatform.googleapis.com/v1/projects/YOUR_PROJECT/locations/us-east5/publishers/anthropic/models"
api_key = "YOUR_GCLOUD_TOKEN"
default_model = "claude-sonnet-4@20250514"
```

```bash
claudex-config profile test vertex-ai
```

<Aside type="caution">
  Die Ausgabe von `gcloud auth print-access-token` laeuft nach ca. 1 Stunde ab. Fuer den Produktiveinsatz ein Dienstkonto mit automatischer Token-Aktualisierung in Betracht ziehen.
</Aside>

### AWS Bedrock (ueber LiteLLM)

AWS Bedrock wird ueber einen LiteLLM-Proxy unterstuetzt:

1. LiteLLM installieren: `pip install litellm`
2. Den Proxy starten:

```bash
litellm --model bedrock/anthropic.claude-sonnet-4-20250514-v2:0
```

3. Claudex konfigurieren:

```toml
[[profiles]]
name = "bedrock"
provider_type = "OpenAICompatible"
base_url = "http://localhost:4000/v1"
api_key = "sk-litellm"
default_model = "bedrock/anthropic.claude-sonnet-4-20250514-v2:0"
```

## OAuth-Abonnements

Die OAuth-Authentifizierung ermoeglicht die Verwendung vorhandener Anbieterabonnements (ChatGPT Plus, Claude Max usw.) anstelle separater API-Schluessel.

### Claude Max / Pro

Das vorhandene Claude-Abonnement ueber die native OAuth-Sitzung von Claude Code verwenden.

**Voraussetzungen**: Claude Code installiert und angemeldet (der `claude`-Befehl funktioniert normal)

```toml
[[profiles]]
name = "claude-max"
provider_type = "DirectAnthropic"
base_url = "https://api.claude.ai"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "claude"

[profiles.models]
haiku = "claude-haiku-4-20250514"
sonnet = "claude-sonnet-4-20250514"
opus = "claude-opus-4-20250514"
```

<Aside type="note">
  Claude OAuth-Profile verwenden einen speziellen Gateway-Auth-Modus. Claudex ueberspringt den Proxy und laesst Claude Code sein eigenes OAuth direkt verwenden. Kein separater `claudex-config auth login`-Schritt erforderlich.
</Aside>

### ChatGPT Plus / Codex CLI

Das ChatGPT Plus- oder Pro-Abonnement ueber den Codex CLI-Token verwenden.

**Voraussetzungen**: [Codex CLI](https://github.com/openai/codex) installieren und authentifizieren:

```bash
# Codex CLI installieren
npm install -g @openai/codex

# Authentifizieren (oeffnet Browser)
codex auth
```

Dann Claudex konfigurieren:

```toml
[[profiles]]
name = "codex-sub"
provider_type = "OpenAIResponses"
base_url = "https://chatgpt.com/backend-api/codex"
default_model = "gpt-5.5"
auth_type = "oauth"
oauth_provider = "openai"

[profiles.models]
haiku = "gpt-5.5"
sonnet = "gpt-5.5"
opus = "gpt-5.5"
```

```bash
# Token aus Codex CLI lesen
claudex-config auth login openai --profile codex-sub

# Ueberpruefen
claudex-config auth status

# Ausfuehren
CLAUDEX_PROFILE=codex-sub claudex
```

<Aside type="tip">
  Unterstuetzte Modelle auf dem Codex-Endpunkt: `gpt-5.5`. Beachten Sie, dass `gpt-4o` auf dem `chatgpt.com`-Codex-Endpunkt NICHT unterstuetzt wird. `strip_params = "auto"` erkennt diesen Endpunkt automatisch und entfernt `temperature`/`top_p`/`top_k`.
</Aside>

### Google Gemini

Google-Konto ueber Gemini CLI-Anmeldedaten verwenden.

```toml
[[profiles]]
name = "gemini-sub"
provider_type = "OpenAICompatible"
base_url = "https://generativelanguage.googleapis.com/v1beta/openai"
default_model = "gemini-2.5-pro"
auth_type = "oauth"
oauth_provider = "google"

[profiles.models]
haiku = "gemini-2.0-flash"
sonnet = "gemini-2.5-pro"
opus = "gemini-2.5-pro"
```

```bash
# Login liest aus Gemini CLI-Anmeldedaten
claudex-config auth login google --profile gemini-sub

# Ueberpruefen
claudex-config auth status
```

### Qwen / Tongyi Qianwen

Qwen-Konto ueber OAuth Device Code Flow verwenden.

```toml
[[profiles]]
name = "qwen-oauth"
provider_type = "OpenAICompatible"
base_url = "https://chat.qwen.ai/api"
default_model = "qwen3-235b-a22b"
auth_type = "oauth"
oauth_provider = "qwen"
```

```bash
# Device Code Flow starten
claudex-config auth login qwen --profile qwen-oauth

# Ueberpruefen
claudex-config auth status
```

### Kimi / Moonshot (OAuth)

Kimi-Konto ueber Kimi CLI-Anmeldedaten verwenden.

```toml
[[profiles]]
name = "kimi-oauth"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.cn/v1"
default_model = "moonshot-v1-128k"
auth_type = "oauth"
oauth_provider = "kimi"
```

```bash
# Login liest aus Kimi CLI-Anmeldedaten
claudex-config auth login kimi --profile kimi-oauth

# Ueberpruefen
claudex-config auth status
```

### GitHub Copilot

GitHub Copilot-Abonnement ueber OAuth Device Code Flow verwenden.

```toml
[[profiles]]
name = "copilot"
provider_type = "OpenAICompatible"
base_url = "https://api.githubcopilot.com"
default_model = "gpt-4o"
auth_type = "oauth"
oauth_provider = "github"
```

```bash
# Device Code Flow starten (oeffnet Browser fuer GitHub-Anmeldung)
claudex-config auth login github --profile copilot

# Ueberpruefen
claudex-config auth status
```

### GitLab Duo

GitLab Duo-Abonnement ueber einen Personal Access Token verwenden.

```toml
[[profiles]]
name = "gitlab-duo"
provider_type = "OpenAICompatible"
base_url = "https://gitlab.com/api/v4/ai/llm/proxy"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "gitlab"
```

```bash
# GitLab-Token als Umgebungsvariable setzen
export GITLAB_TOKEN=glpat-...

# Login
claudex-config auth login gitlab --profile gitlab-duo

# Ueberpruefen
claudex-config auth status
```

<Aside type="note">
  GitLab Duo erfordert eine `GITLAB_TOKEN`-Umgebungsvariable mit einem Personal Access Token, der den `ai_features`-Scope hat. Fuer selbst-gehostete GitLab-Instanzen `base_url` entsprechend anpassen.
</Aside>

## Lokale Modelle

### Ollama

1. Ollama installieren: [ollama.com](https://ollama.com/)
2. Ein Modell herunterladen:

```bash
ollama pull qwen2.5:72b
```

3. Claudex konfigurieren:

```toml
[[profiles]]
name = "local-qwen"
provider_type = "OpenAICompatible"
base_url = "http://localhost:11434/v1"
api_key = ""
default_model = "qwen2.5:72b"
```

```bash
claudex-config profile test local-qwen
```

<Aside type="note">
  Ollama benoetigt keinen API-Schluessel. `api_key` leer lassen oder weglassen.
</Aside>

### vLLM

1. vLLM installieren und starten:

```bash
pip install vllm
vllm serve meta-llama/Llama-3.3-70B-Instruct --port 8000
```

2. Claudex konfigurieren:

```toml
[[profiles]]
name = "local-llama"
provider_type = "OpenAICompatible"
base_url = "http://localhost:8000/v1"
api_key = ""
default_model = "meta-llama/Llama-3.3-70B-Instruct"
```

### LM Studio

1. LM Studio von [lmstudio.ai](https://lmstudio.ai/) herunterladen
2. Ein Modell laden und den lokalen Server starten (Standardport: 1234)

```toml
[[profiles]]
name = "lm-studio"
provider_type = "OpenAICompatible"
base_url = "http://localhost:1234/v1"
api_key = "lm-studio"
default_model = "local-model"
```

## Einrichtung ueberpruefen

Nach der Konfiguration eines Anbieters die Verbindung ueberpruefen:

```bash
# Ein bestimmtes Profil testen
claudex-config profile test <profile-name>

# Alle Profile testen
claudex-config profile test all

# Alle konfigurierten Profile auflisten
claudex-config profile list
```
