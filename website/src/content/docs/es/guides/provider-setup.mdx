---
title: Guia de configuracion de proveedores
description: Instrucciones paso a paso para configurar cada proveedor de IA con Claudex
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Esta guia cubre instrucciones detalladas de configuracion para cada proveedor soportado por Claudex, incluyendo proveedores de clave API, suscripciones OAuth, plataformas en la nube y modelos locales.

## Proveedores con clave API

### Anthropic

1. Registrate en [console.anthropic.com](https://console.anthropic.com/)
2. Navega a **API Keys** y crea una nueva clave (formato: `sk-ant-*`)

```toml
[[profiles]]
name = "anthropic"
provider_type = "DirectAnthropic"
base_url = "https://api.anthropic.com"
api_key = "sk-ant-..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex profile test anthropic
```

### MiniMax

1. Registrate en [platform.minimaxi.com](https://platform.minimaxi.com/)
2. Navega a **API Keys** en la consola

```toml
[[profiles]]
name = "minimax"
provider_type = "DirectAnthropic"
base_url = "https://api.minimax.io/anthropic"
api_key = "..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex profile test minimax
```

### OpenRouter

1. Registrate en [openrouter.ai](https://openrouter.ai/)
2. Obtener tu clave API en [openrouter.ai/keys](https://openrouter.ai/keys)

```toml
[[profiles]]
name = "openrouter"
provider_type = "OpenAICompatible"
base_url = "https://openrouter.ai/api/v1"
api_key = "sk-or-..."
default_model = "anthropic/claude-sonnet-4"
```

```bash
claudex profile test openrouter
```

<Aside type="tip">
  OpenRouter soporta cientos de modelos de diferentes proveedores. Usa el prefijo `anthropic/` para los modelos Claude.
</Aside>

### Grok (xAI)

1. Registrate en [console.x.ai](https://console.x.ai/)
2. Crea una clave API en la consola

```toml
[[profiles]]
name = "grok"
provider_type = "OpenAICompatible"
base_url = "https://api.x.ai/v1"
api_key = "xai-..."
default_model = "grok-3-beta"
```

```bash
claudex profile test grok
```

### OpenAI

1. Registrate en [platform.openai.com](https://platform.openai.com/)
2. Crea una clave API en [platform.openai.com/api-keys](https://platform.openai.com/api-keys)

```toml
[[profiles]]
name = "chatgpt"
provider_type = "OpenAICompatible"
base_url = "https://api.openai.com/v1"
api_key = "sk-..."
default_model = "gpt-4o"
```

```bash
claudex profile test chatgpt
```

### DeepSeek

1. Registrate en [platform.deepseek.com](https://platform.deepseek.com/)
2. Crea una clave API en la consola

```toml
[[profiles]]
name = "deepseek"
provider_type = "OpenAICompatible"
base_url = "https://api.deepseek.com"
api_key = "sk-..."
default_model = "deepseek-chat"
```

```bash
claudex profile test deepseek
```

### Kimi / Moonshot

1. Registrate en [platform.moonshot.cn](https://platform.moonshot.cn/)
2. Crea una clave API en la consola

```toml
[[profiles]]
name = "kimi"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.ai/v1"
api_key = "sk-..."
default_model = "kimi-k2-0905-preview"
```

```bash
claudex profile test kimi
```

### GLM / Zhipu

1. Registrate en [open.bigmodel.cn](https://open.bigmodel.cn/)
2. Crea una clave API en la consola

```toml
[[profiles]]
name = "glm"
provider_type = "OpenAICompatible"
base_url = "https://api.z.ai/api/paas/v4"
api_key = "..."
default_model = "glm-4.6"
```

```bash
claudex profile test glm
```

### Groq

1. Registrate en [console.groq.com](https://console.groq.com/)
2. Crea una clave API en **Settings > API Keys**

```toml
[[profiles]]
name = "groq"
provider_type = "OpenAICompatible"
base_url = "https://api.groq.com/openai/v1"
api_key = "gsk_..."
default_model = "llama-3.3-70b-versatile"
```

```bash
claudex profile test groq
```

<Aside type="tip">
  Groq ofrece inferencia extremadamente rapida para modelos open-source. Los modelos populares incluyen `llama-3.3-70b-versatile`, `mixtral-8x7b-32768` y `gemma2-9b-it`.
</Aside>

### Mistral AI

1. Registrate en [console.mistral.ai](https://console.mistral.ai/)
2. Crea una clave API en la consola

```toml
[[profiles]]
name = "mistral"
provider_type = "OpenAICompatible"
base_url = "https://api.mistral.ai/v1"
api_key = "..."
default_model = "mistral-large-latest"
```

```bash
claudex profile test mistral
```

### Together AI

1. Registrate en [api.together.ai](https://api.together.ai/)
2. Crea una clave API en el panel de control

```toml
[[profiles]]
name = "together"
provider_type = "OpenAICompatible"
base_url = "https://api.together.xyz/v1"
api_key = "..."
default_model = "meta-llama/Llama-3.3-70B-Instruct-Turbo"
```

```bash
claudex profile test together
```

### Perplexity

1. Registrate en [perplexity.ai](https://www.perplexity.ai/)
2. Crea una clave API en [perplexity.ai/settings/api](https://www.perplexity.ai/settings/api)

```toml
[[profiles]]
name = "perplexity"
provider_type = "OpenAICompatible"
base_url = "https://api.perplexity.ai"
api_key = "pplx-..."
default_model = "sonar-pro"
```

```bash
claudex profile test perplexity
```

<Aside type="note">
  Los modelos de Perplexity tienen capacidades integradas de busqueda web. Util para tareas orientadas a la investigacion.
</Aside>

### Cerebras

1. Registrate en [cloud.cerebras.ai](https://cloud.cerebras.ai/)
2. Crea una clave API en el panel de control

```toml
[[profiles]]
name = "cerebras"
provider_type = "OpenAICompatible"
base_url = "https://api.cerebras.ai/v1"
api_key = "..."
default_model = "llama-3.3-70b"
```

```bash
claudex profile test cerebras
```

## Plataformas en la nube

### Azure OpenAI

1. Crea un recurso Azure OpenAI en el [Azure Portal](https://portal.azure.com/)
2. Despliega un modelo y anota el nombre del recurso y del despliegue
3. Obtener tu clave API en **Keys and Endpoint**

```toml
[[profiles]]
name = "azure-openai"
provider_type = "OpenAICompatible"
base_url = "https://YOUR_RESOURCE.openai.azure.com/openai/deployments/YOUR_DEPLOYMENT"
api_key = "YOUR_AZURE_KEY"
default_model = "gpt-4o"

[profiles.query_params]
api-version = "2024-12-01-preview"
```

```bash
claudex profile test azure-openai
```

<Aside type="note">
  Azure OpenAI se detecta automaticamente por `base_url` que contenga `openai.azure.com`. La autenticacion usa la cabecera `api-key` en lugar de `Authorization: Bearer`. El parametro de consulta `api-version` es obligatorio.
</Aside>

### Google Vertex AI

1. Habilita la API de Vertex AI en tu proyecto GCP
2. Genera un token de acceso con `gcloud auth print-access-token`

```toml
[[profiles]]
name = "vertex-ai"
provider_type = "DirectAnthropic"
base_url = "https://us-east5-aiplatform.googleapis.com/v1/projects/YOUR_PROJECT/locations/us-east5/publishers/anthropic/models"
api_key = "YOUR_GCLOUD_TOKEN"
default_model = "claude-sonnet-4@20250514"
```

```bash
claudex profile test vertex-ai
```

<Aside type="caution">
  La salida de `gcloud auth print-access-token` expira despues de ~1 hora. Para uso en produccion, considera una cuenta de servicio con automatizacion de refresco de tokens.
</Aside>

### AWS Bedrock (via LiteLLM)

AWS Bedrock se soporta a traves de un proxy LiteLLM:

1. Instala LiteLLM: `pip install litellm`
2. Inicia el proxy:

```bash
litellm --model bedrock/anthropic.claude-sonnet-4-20250514-v2:0
```

3. Configura Claudex:

```toml
[[profiles]]
name = "bedrock"
provider_type = "OpenAICompatible"
base_url = "http://localhost:4000/v1"
api_key = "sk-litellm"
default_model = "bedrock/anthropic.claude-sonnet-4-20250514-v2:0"
```

## Suscripciones OAuth

La autenticacion OAuth te permite usar suscripciones existentes de proveedores (ChatGPT Plus, Claude Max, etc.) en lugar de claves API separadas.

### Claude Max / Pro

Usa tu suscripcion existente de Claude a traves de la sesion OAuth nativa de Claude Code.

**Requisitos previos**: Claude Code instalado e iniciado sesion (el comando `claude` funciona normalmente)

```toml
[[profiles]]
name = "claude-max"
provider_type = "DirectAnthropic"
base_url = "https://api.claude.ai"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "claude"

[profiles.models]
haiku = "claude-haiku-4-20250514"
sonnet = "claude-sonnet-4-20250514"
opus = "claude-opus-4-20250514"
```

<Aside type="note">
  Los perfiles OAuth de Claude usan un modo especial de gateway auth. Claudex omite el proxy y deja que Claude Code use su propio OAuth directamente. No se necesita un paso separado de `claudex auth login`.
</Aside>

### ChatGPT Plus / Codex CLI

Usa tu suscripcion de ChatGPT Plus o Pro mediante el token del Codex CLI.

**Requisitos previos**: Instala [Codex CLI](https://github.com/openai/codex) y autenticate:

```bash
# Instalar Codex CLI
npm install -g @openai/codex

# Autenticar (abre el navegador)
codex auth
```

Luego configura Claudex:

```toml
[[profiles]]
name = "codex-sub"
provider_type = "OpenAIResponses"
base_url = "https://chatgpt.com/backend-api/codex"
default_model = "gpt-5.5"
auth_type = "oauth"
oauth_provider = "openai"

[profiles.models]
haiku = "gpt-5.5"
sonnet = "gpt-5.5"
opus = "gpt-5.5"
```

```bash
# Leer token desde Codex CLI
claudex auth login openai --profile codex-sub

# Verificar
claudex auth status

# Ejecutar
claudex run codex-sub
```

<Aside type="tip">
  Modelos soportados en el endpoint Codex: `gpt-5.5`. Ten en cuenta que `gpt-4o` NO esta soportado en el endpoint Codex de `chatgpt.com`. `strip_params = "auto"` detectara automaticamente este endpoint y eliminara `temperature`/`top_p`/`top_k`.
</Aside>

### Google Gemini

Usa tu cuenta de Google mediante las credenciales de Gemini CLI.

```toml
[[profiles]]
name = "gemini-sub"
provider_type = "OpenAICompatible"
base_url = "https://generativelanguage.googleapis.com/v1beta/openai"
default_model = "gemini-2.5-pro"
auth_type = "oauth"
oauth_provider = "google"

[profiles.models]
haiku = "gemini-2.0-flash"
sonnet = "gemini-2.5-pro"
opus = "gemini-2.5-pro"
```

```bash
# Login lee de las credenciales de Gemini CLI
claudex auth login google --profile gemini-sub

# Verificar
claudex auth status
```

### Qwen / Tongyi Qianwen

Usa tu cuenta de Qwen mediante el flujo OAuth Device Code.

```toml
[[profiles]]
name = "qwen-oauth"
provider_type = "OpenAICompatible"
base_url = "https://chat.qwen.ai/api"
default_model = "qwen3-235b-a22b"
auth_type = "oauth"
oauth_provider = "qwen"
```

```bash
# Iniciar flujo de codigo de dispositivo
claudex auth login qwen --profile qwen-oauth

# Verificar
claudex auth status
```

### Kimi / Moonshot (OAuth)

Usa tu cuenta de Kimi mediante las credenciales de Kimi CLI.

```toml
[[profiles]]
name = "kimi-oauth"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.cn/v1"
default_model = "moonshot-v1-128k"
auth_type = "oauth"
oauth_provider = "kimi"
```

```bash
# Login lee de las credenciales de Kimi CLI
claudex auth login kimi --profile kimi-oauth

# Verificar
claudex auth status
```

### GitHub Copilot

Usa tu suscripcion de GitHub Copilot mediante el flujo OAuth Device Code.

```toml
[[profiles]]
name = "copilot"
provider_type = "OpenAICompatible"
base_url = "https://api.githubcopilot.com"
default_model = "gpt-4o"
auth_type = "oauth"
oauth_provider = "github"
```

```bash
# Iniciar flujo de codigo de dispositivo (abre el navegador para inicio de sesion en GitHub)
claudex auth login github --profile copilot

# Verificar
claudex auth status
```

### GitLab Duo

Usa tu suscripcion de GitLab Duo mediante un Personal Access Token.

```toml
[[profiles]]
name = "gitlab-duo"
provider_type = "OpenAICompatible"
base_url = "https://gitlab.com/api/v4/ai/llm/proxy"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "gitlab"
```

```bash
# Establece tu token de GitLab como variable de entorno
export GITLAB_TOKEN=glpat-...

# Login
claudex auth login gitlab --profile gitlab-duo

# Verificar
claudex auth status
```

<Aside type="note">
  GitLab Duo requiere una variable de entorno `GITLAB_TOKEN` con un Personal Access Token que tenga el scope `ai_features`. Para instancias GitLab autogestionadas, actualiza `base_url` correspondentemente.
</Aside>

## Modelos locales

### Ollama

1. Instala Ollama: [ollama.com](https://ollama.com/)
2. Descarga un modelo:

```bash
ollama pull qwen2.5:72b
```

3. Configura Claudex:

```toml
[[profiles]]
name = "local-qwen"
provider_type = "OpenAICompatible"
base_url = "http://localhost:11434/v1"
api_key = ""
default_model = "qwen2.5:72b"
```

```bash
claudex profile test local-qwen
```

<Aside type="note">
  Ollama no requiere clave API. Deja `api_key` vacio u omitelo.
</Aside>

### vLLM

1. Instala e inicia vLLM:

```bash
pip install vllm
vllm serve meta-llama/Llama-3.3-70B-Instruct --port 8000
```

2. Configura Claudex:

```toml
[[profiles]]
name = "local-llama"
provider_type = "OpenAICompatible"
base_url = "http://localhost:8000/v1"
api_key = ""
default_model = "meta-llama/Llama-3.3-70B-Instruct"
```

### LM Studio

1. Descarga LM Studio desde [lmstudio.ai](https://lmstudio.ai/)
2. Carga un modelo e inicia el servidor local (puerto por defecto: 1234)

```toml
[[profiles]]
name = "lm-studio"
provider_type = "OpenAICompatible"
base_url = "http://localhost:1234/v1"
api_key = "lm-studio"
default_model = "local-model"
```

## Verificar tu configuracion

Despues de configurar cualquier proveedor, verifica la conectividad:

```bash
# Probar un perfil especifico
claudex profile test <profile-name>

# Probar todos los perfiles
claudex profile test all

# Listar todos los perfiles configurados
claudex profile list
```
