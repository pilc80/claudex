---
title: Installazione
description: Come installare Claudex sul tuo sistema
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

## Metodi di installazione

<Tabs>
  <TabItem label="One-liner" icon="terminal">
    ```bash
    curl -fsSL https://raw.githubusercontent.com/pilc80/claudex/main/install.sh | bash
    ```
    Lo script di installazione rileva automaticamente il sistema operativo e l'architettura, scarica l'ultima release da GitHub e installa in `~/.local/bin/claudex`.

    Per specificare una directory di installazione diversa:
    ```bash
    CLAUDEX_INSTALL_DIR=/usr/local/bin curl -fsSL https://raw.githubusercontent.com/pilc80/claudex/main/install.sh | bash
    ```
  </TabItem>
  <TabItem label="Cargo" icon="seti:rust">
    ```bash
    cargo install --git https://github.com/pilc80/claudex
    ```
    Richiede la toolchain Rust. Il binario verra installato in `~/.cargo/bin/claudex`.
  </TabItem>
  <TabItem label="GitHub Releases" icon="github">
    Scarica il binario precompilato per la tua piattaforma da [GitHub Releases](https://github.com/pilc80/claudex/releases).

    Target disponibili:
    - `x86_64-unknown-linux-gnu`
    - `x86_64-unknown-linux-musl`
    - `aarch64-unknown-linux-gnu`
    - `aarch64-unknown-linux-musl`
    - `x86_64-apple-darwin`
    - `aarch64-apple-darwin`
    - `x86_64-pc-windows-msvc`

    Estrai e posiziona il binario nel tuo `PATH`:
    ```bash
    tar xzf claudex-v*.tar.gz
    chmod +x claudex
    mv claudex ~/.local/bin/
    ```
  </TabItem>
</Tabs>

## Requisiti di sistema

| Requisito | Dettagli |
|-----------|----------|
| **SO** | macOS (Intel / Apple Silicon), Linux (x86_64 / ARM64), Windows (x86_64) |
| **Claude Code** | Deve essere installato e accessibile come `claude` nel PATH |
| **Opzionale** | [Ollama](https://ollama.com) per le funzionalita di routing intelligente e motore di contesto |

## Verifica dell'installazione

```bash
claudex --version
```

## Prossimi passi

Dopo l'installazione, [configura il tuo primo profilo fornitore](/it/configuration/).
