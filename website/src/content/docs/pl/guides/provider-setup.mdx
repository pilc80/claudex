---
title: Przewodnik konfiguracji dostawcy
description: Szczegolowe instrukcje konfiguracji kazdego dostawcy AI z Claudex
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Ten przewodnik zawiera szczegolowe instrukcje konfiguracji dla kazdego dostawcy obslugiwanego przez Claudex, w tym dostawcow kluczy API, subskrypcji OAuth, platform chmurowych i modeli lokalnych.

## Dostawcy kluczy API

### Anthropic

1. Zarejestruj sie na [console.anthropic.com](https://console.anthropic.com/)
2. Przejdz do **API Keys** i utworz nowy klucz (format: `sk-ant-*`)

```toml
[[profiles]]
name = "anthropic"
provider_type = "DirectAnthropic"
base_url = "https://api.anthropic.com"
api_key = "sk-ant-..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex profile test anthropic
```

### MiniMax

1. Zarejestruj sie na [platform.minimaxi.com](https://platform.minimaxi.com/)
2. Przejdz do **API Keys** w konsoli

```toml
[[profiles]]
name = "minimax"
provider_type = "DirectAnthropic"
base_url = "https://api.minimax.io/anthropic"
api_key = "..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex profile test minimax
```

### OpenRouter

1. Zarejestruj sie na [openrouter.ai](https://openrouter.ai/)
2. Pobierz klucz API na [openrouter.ai/keys](https://openrouter.ai/keys)

```toml
[[profiles]]
name = "openrouter"
provider_type = "OpenAICompatible"
base_url = "https://openrouter.ai/api/v1"
api_key = "sk-or-..."
default_model = "anthropic/claude-sonnet-4"
```

```bash
claudex profile test openrouter
```

<Aside type="tip">
  OpenRouter obsluguje setki modeli od roznych dostawcow. Uzyj prefiksu `anthropic/` dla modeli Claude.
</Aside>

### Grok (xAI)

1. Zarejestruj sie na [console.x.ai](https://console.x.ai/)
2. Utworz klucz API w konsoli

```toml
[[profiles]]
name = "grok"
provider_type = "OpenAICompatible"
base_url = "https://api.x.ai/v1"
api_key = "xai-..."
default_model = "grok-3-beta"
```

```bash
claudex profile test grok
```

### OpenAI

1. Zarejestruj sie na [platform.openai.com](https://platform.openai.com/)
2. Utworz klucz API na [platform.openai.com/api-keys](https://platform.openai.com/api-keys)

```toml
[[profiles]]
name = "chatgpt"
provider_type = "OpenAICompatible"
base_url = "https://api.openai.com/v1"
api_key = "sk-..."
default_model = "gpt-4o"
```

```bash
claudex profile test chatgpt
```

### DeepSeek

1. Zarejestruj sie na [platform.deepseek.com](https://platform.deepseek.com/)
2. Utworz klucz API w konsoli

```toml
[[profiles]]
name = "deepseek"
provider_type = "OpenAICompatible"
base_url = "https://api.deepseek.com"
api_key = "sk-..."
default_model = "deepseek-chat"
```

```bash
claudex profile test deepseek
```

### Kimi / Moonshot

1. Zarejestruj sie na [platform.moonshot.cn](https://platform.moonshot.cn/)
2. Utworz klucz API w konsoli

```toml
[[profiles]]
name = "kimi"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.ai/v1"
api_key = "sk-..."
default_model = "kimi-k2-0905-preview"
```

```bash
claudex profile test kimi
```

### GLM / Zhipu

1. Zarejestruj sie na [open.bigmodel.cn](https://open.bigmodel.cn/)
2. Utworz klucz API w konsoli

```toml
[[profiles]]
name = "glm"
provider_type = "OpenAICompatible"
base_url = "https://api.z.ai/api/paas/v4"
api_key = "..."
default_model = "glm-4.6"
```

```bash
claudex profile test glm
```

### Groq

1. Zarejestruj sie na [console.groq.com](https://console.groq.com/)
2. Utworz klucz API w **Settings > API Keys**

```toml
[[profiles]]
name = "groq"
provider_type = "OpenAICompatible"
base_url = "https://api.groq.com/openai/v1"
api_key = "gsk_..."
default_model = "llama-3.3-70b-versatile"
```

```bash
claudex profile test groq
```

<Aside type="tip">
  Groq zapewnia bardzo szybka inferencje dla modeli open-source. Popularne modele to `llama-3.3-70b-versatile`, `mixtral-8x7b-32768` i `gemma2-9b-it`.
</Aside>

### Mistral AI

1. Zarejestruj sie na [console.mistral.ai](https://console.mistral.ai/)
2. Utworz klucz API w konsoli

```toml
[[profiles]]
name = "mistral"
provider_type = "OpenAICompatible"
base_url = "https://api.mistral.ai/v1"
api_key = "..."
default_model = "mistral-large-latest"
```

```bash
claudex profile test mistral
```

### Together AI

1. Zarejestruj sie na [api.together.ai](https://api.together.ai/)
2. Utworz klucz API w panelu

```toml
[[profiles]]
name = "together"
provider_type = "OpenAICompatible"
base_url = "https://api.together.xyz/v1"
api_key = "..."
default_model = "meta-llama/Llama-3.3-70B-Instruct-Turbo"
```

```bash
claudex profile test together
```

### Perplexity

1. Zarejestruj sie na [perplexity.ai](https://www.perplexity.ai/)
2. Utworz klucz API na [perplexity.ai/settings/api](https://www.perplexity.ai/settings/api)

```toml
[[profiles]]
name = "perplexity"
provider_type = "OpenAICompatible"
base_url = "https://api.perplexity.ai"
api_key = "pplx-..."
default_model = "sonar-pro"
```

```bash
claudex profile test perplexity
```

<Aside type="note">
  Modele Perplexity maja wbudowane mozliwosci przeszukiwania internetu. Przydatne do zadan badawczych.
</Aside>

### Cerebras

1. Zarejestruj sie na [cloud.cerebras.ai](https://cloud.cerebras.ai/)
2. Utworz klucz API w panelu

```toml
[[profiles]]
name = "cerebras"
provider_type = "OpenAICompatible"
base_url = "https://api.cerebras.ai/v1"
api_key = "..."
default_model = "llama-3.3-70b"
```

```bash
claudex profile test cerebras
```

## Platformy chmurowe

### Azure OpenAI

1. Utworz zasob Azure OpenAI w [Azure Portal](https://portal.azure.com/)
2. Wdraz model i zapisz nazwe zasobu oraz nazwe wdrozenia
3. Pobierz klucz API z **Keys and Endpoint**

```toml
[[profiles]]
name = "azure-openai"
provider_type = "OpenAICompatible"
base_url = "https://YOUR_RESOURCE.openai.azure.com/openai/deployments/YOUR_DEPLOYMENT"
api_key = "YOUR_AZURE_KEY"
default_model = "gpt-4o"

[profiles.query_params]
api-version = "2024-12-01-preview"
```

```bash
claudex profile test azure-openai
```

<Aside type="note">
  Azure OpenAI jest automatycznie wykrywany po `base_url` zawierajacym `openai.azure.com`. Uwierzytelnianie uzywa naglowka `api-key` zamiast `Authorization: Bearer`. Parametr zapytania `api-version` jest wymagany.
</Aside>

### Google Vertex AI

1. Wlacz API Vertex AI w swoim projekcie GCP
2. Wygeneruj token dostepu za pomoca `gcloud auth print-access-token`

```toml
[[profiles]]
name = "vertex-ai"
provider_type = "DirectAnthropic"
base_url = "https://us-east5-aiplatform.googleapis.com/v1/projects/YOUR_PROJECT/locations/us-east5/publishers/anthropic/models"
api_key = "YOUR_GCLOUD_TOKEN"
default_model = "claude-sonnet-4@20250514"
```

```bash
claudex profile test vertex-ai
```

<Aside type="caution">
  Wyjscie `gcloud auth print-access-token` wygasa po okolo 1 godzinie. Do uzycia produkcyjnego rozwazyc konto uslugowe z automatycznym odswiezaniem tokenow.
</Aside>

### AWS Bedrock (przez LiteLLM)

AWS Bedrock jest obslugiwany przez proxy LiteLLM:

1. Zainstaluj LiteLLM: `pip install litellm`
2. Uruchom proxy:

```bash
litellm --model bedrock/anthropic.claude-sonnet-4-20250514-v2:0
```

3. Skonfiguruj Claudex:

```toml
[[profiles]]
name = "bedrock"
provider_type = "OpenAICompatible"
base_url = "http://localhost:4000/v1"
api_key = "sk-litellm"
default_model = "bedrock/anthropic.claude-sonnet-4-20250514-v2:0"
```

## Subskrypcje OAuth

Uwierzytelnianie OAuth pozwala uzywac istniejacych subskrypcji dostawcy (ChatGPT Plus, Claude Max itp.) zamiast osobnych kluczy API.

### Claude Max / Pro

Uzyj istniejacej subskrypcji Claude przez natywna sesje OAuth Claude Code.

**Wymagania wstepne**: Claude Code zainstalowany i zalogowany (polecenie `claude` dziala normalnie)

```toml
[[profiles]]
name = "claude-max"
provider_type = "DirectAnthropic"
base_url = "https://api.claude.ai"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "claude"

[profiles.models]
haiku = "claude-haiku-4-20250514"
sonnet = "claude-sonnet-4-20250514"
opus = "claude-opus-4-20250514"
```

<Aside type="note">
  Profile OAuth Claude uzywaja specjalnego trybu uwierzytelniania bramkowego. Claudex pomija proxy i pozwala Claude Code uzywac wlasnego OAuth bezposrednio. Nie jest potrzebny osobny krok `claudex auth login`.
</Aside>

### ChatGPT Plus / Codex CLI

Uzyj subskrypcji ChatGPT Plus lub Pro przez token Codex CLI.

**Wymagania wstepne**: Zainstaluj [Codex CLI](https://github.com/openai/codex) i uwierzytelnij sie:

```bash
# Zainstaluj Codex CLI
npm install -g @openai/codex

# Uwierzytelnianie (otwiera przegladarke)
codex auth
```

Nastepnie skonfiguruj Claudex:

```toml
[[profiles]]
name = "codex-sub"
provider_type = "OpenAIResponses"
base_url = "https://chatgpt.com/backend-api/codex"
default_model = "gpt-5.5"
auth_type = "oauth"
oauth_provider = "openai"

[profiles.models]
haiku = "gpt-5.5"
sonnet = "gpt-5.5"
opus = "gpt-5.5"
```

```bash
# Odczytaj token z Codex CLI
claudex auth login openai --profile codex-sub

# Weryfikacja
claudex auth status

# Uruchomienie
claudex run codex-sub
```

<Aside type="tip">
  Obslugiwane modele na endpoincie Codex: `gpt-5.5`. Model `gpt-4o` NIE jest obslugiwany na endpoincie `chatgpt.com` Codex. `strip_params = "auto"` automatycznie wykryje ten endpoint i usunie `temperature`/`top_p`/`top_k`.
</Aside>

### Google Gemini

Uzyj konta Google przez poswiadczenia Gemini CLI.

```toml
[[profiles]]
name = "gemini-sub"
provider_type = "OpenAICompatible"
base_url = "https://generativelanguage.googleapis.com/v1beta/openai"
default_model = "gemini-2.5-pro"
auth_type = "oauth"
oauth_provider = "google"

[profiles.models]
haiku = "gemini-2.0-flash"
sonnet = "gemini-2.5-pro"
opus = "gemini-2.5-pro"
```

```bash
# Login odczytuje poswiadczenia z Gemini CLI
claudex auth login google --profile gemini-sub

# Weryfikacja
claudex auth status
```

### Qwen / Tongyi Qianwen

Uzyj konta Qwen przez przeplyw OAuth Device Code.

```toml
[[profiles]]
name = "qwen-oauth"
provider_type = "OpenAICompatible"
base_url = "https://chat.qwen.ai/api"
default_model = "qwen3-235b-a22b"
auth_type = "oauth"
oauth_provider = "qwen"
```

```bash
# Uruchom przeplyw device code
claudex auth login qwen --profile qwen-oauth

# Weryfikacja
claudex auth status
```

### Kimi / Moonshot (OAuth)

Uzyj konta Kimi przez poswiadczenia Kimi CLI.

```toml
[[profiles]]
name = "kimi-oauth"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.cn/v1"
default_model = "moonshot-v1-128k"
auth_type = "oauth"
oauth_provider = "kimi"
```

```bash
# Login odczytuje poswiadczenia z Kimi CLI
claudex auth login kimi --profile kimi-oauth

# Weryfikacja
claudex auth status
```

### GitHub Copilot

Uzyj subskrypcji GitHub Copilot przez przeplyw OAuth Device Code.

```toml
[[profiles]]
name = "copilot"
provider_type = "OpenAICompatible"
base_url = "https://api.githubcopilot.com"
default_model = "gpt-4o"
auth_type = "oauth"
oauth_provider = "github"
```

```bash
# Uruchom przeplyw device code (otwiera przegladarke do logowania GitHub)
claudex auth login github --profile copilot

# Weryfikacja
claudex auth status
```

### GitLab Duo

Uzyj subskrypcji GitLab Duo przez Personal Access Token.

```toml
[[profiles]]
name = "gitlab-duo"
provider_type = "OpenAICompatible"
base_url = "https://gitlab.com/api/v4/ai/llm/proxy"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "gitlab"
```

```bash
# Ustaw token GitLab jako zmienna srodowiskowa
export GITLAB_TOKEN=glpat-...

# Login
claudex auth login gitlab --profile gitlab-duo

# Weryfikacja
claudex auth status
```

<Aside type="note">
  GitLab Duo wymaga zmiennej srodowiskowej `GITLAB_TOKEN` z Personal Access Token majacym scope `ai_features`. Dla samodzielnie hostowanych instancji GitLab zaktualizuj odpowiednio `base_url`.
</Aside>

## Modele lokalne

### Ollama

1. Zainstaluj Ollama: [ollama.com](https://ollama.com/)
2. Pobierz model:

```bash
ollama pull qwen2.5:72b
```

3. Skonfiguruj Claudex:

```toml
[[profiles]]
name = "local-qwen"
provider_type = "OpenAICompatible"
base_url = "http://localhost:11434/v1"
api_key = ""
default_model = "qwen2.5:72b"
```

```bash
claudex profile test local-qwen
```

<Aside type="note">
  Ollama nie wymaga klucza API. Pozostaw `api_key` puste lub pomin je.
</Aside>

### vLLM

1. Zainstaluj i uruchom vLLM:

```bash
pip install vllm
vllm serve meta-llama/Llama-3.3-70B-Instruct --port 8000
```

2. Skonfiguruj Claudex:

```toml
[[profiles]]
name = "local-llama"
provider_type = "OpenAICompatible"
base_url = "http://localhost:8000/v1"
api_key = ""
default_model = "meta-llama/Llama-3.3-70B-Instruct"
```

### LM Studio

1. Pobierz LM Studio z [lmstudio.ai](https://lmstudio.ai/)
2. Zaladuj model i uruchom serwer lokalny (domyslny port: 1234)

```toml
[[profiles]]
name = "lm-studio"
provider_type = "OpenAICompatible"
base_url = "http://localhost:1234/v1"
api_key = "lm-studio"
default_model = "local-model"
```

## Weryfikacja konfiguracji

Po skonfigurowaniu dowolnego dostawcy sprawdz lacznosc:

```bash
# Przetestuj konkretny profil
claudex profile test <profile-name>

# Przetestuj wszystkie profile
claudex profile test all

# Wylistuj wszystkie skonfigurowane profile
claudex profile list
```
