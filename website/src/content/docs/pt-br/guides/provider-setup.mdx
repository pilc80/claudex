---
title: Guia de Configuracao de Provedores
description: Instrucoes passo a passo para configurar cada provedor de IA com o Claudex
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Este guia cobre instrucoes detalhadas de configuracao para todos os provedores suportados pelo Claudex, incluindo provedores com chave API, assinaturas OAuth, plataformas cloud e modelos locais.

## Provedores com Chave API

### Anthropic

1. Cadastre-se em [console.anthropic.com](https://console.anthropic.com/)
2. Navegue ate **API Keys** e crie uma nova chave (formato: `sk-ant-*`)

```toml
[[profiles]]
name = "anthropic"
provider_type = "DirectAnthropic"
base_url = "https://api.anthropic.com"
api_key = "sk-ant-..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex profile test anthropic
```

### MiniMax

1. Cadastre-se em [platform.minimaxi.com](https://platform.minimaxi.com/)
2. Navegue ate **API Keys** no console

```toml
[[profiles]]
name = "minimax"
provider_type = "DirectAnthropic"
base_url = "https://api.minimax.io/anthropic"
api_key = "..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex profile test minimax
```

### OpenRouter

1. Cadastre-se em [openrouter.ai](https://openrouter.ai/)
2. Obtenha sua chave API em [openrouter.ai/keys](https://openrouter.ai/keys)

```toml
[[profiles]]
name = "openrouter"
provider_type = "OpenAICompatible"
base_url = "https://openrouter.ai/api/v1"
api_key = "sk-or-..."
default_model = "anthropic/claude-sonnet-4"
```

```bash
claudex profile test openrouter
```

<Aside type="tip">
  O OpenRouter suporta centenas de modelos de diferentes provedores. Use o prefixo `anthropic/` para modelos Claude.
</Aside>

### Grok (xAI)

1. Cadastre-se em [console.x.ai](https://console.x.ai/)
2. Crie uma chave API no console

```toml
[[profiles]]
name = "grok"
provider_type = "OpenAICompatible"
base_url = "https://api.x.ai/v1"
api_key = "xai-..."
default_model = "grok-3-beta"
```

```bash
claudex profile test grok
```

### OpenAI

1. Cadastre-se em [platform.openai.com](https://platform.openai.com/)
2. Crie uma chave API em [platform.openai.com/api-keys](https://platform.openai.com/api-keys)

```toml
[[profiles]]
name = "chatgpt"
provider_type = "OpenAICompatible"
base_url = "https://api.openai.com/v1"
api_key = "sk-..."
default_model = "gpt-4o"
```

```bash
claudex profile test chatgpt
```

### DeepSeek

1. Cadastre-se em [platform.deepseek.com](https://platform.deepseek.com/)
2. Crie uma chave API no console

```toml
[[profiles]]
name = "deepseek"
provider_type = "OpenAICompatible"
base_url = "https://api.deepseek.com"
api_key = "sk-..."
default_model = "deepseek-chat"
```

```bash
claudex profile test deepseek
```

### Kimi / Moonshot

1. Cadastre-se em [platform.moonshot.cn](https://platform.moonshot.cn/)
2. Crie uma chave API no console

```toml
[[profiles]]
name = "kimi"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.ai/v1"
api_key = "sk-..."
default_model = "kimi-k2-0905-preview"
```

```bash
claudex profile test kimi
```

### GLM / Zhipu

1. Cadastre-se em [open.bigmodel.cn](https://open.bigmodel.cn/)
2. Crie uma chave API no console

```toml
[[profiles]]
name = "glm"
provider_type = "OpenAICompatible"
base_url = "https://api.z.ai/api/paas/v4"
api_key = "..."
default_model = "glm-4.6"
```

```bash
claudex profile test glm
```

### Groq

1. Cadastre-se em [console.groq.com](https://console.groq.com/)
2. Crie uma chave API em **Settings > API Keys**

```toml
[[profiles]]
name = "groq"
provider_type = "OpenAICompatible"
base_url = "https://api.groq.com/openai/v1"
api_key = "gsk_..."
default_model = "llama-3.3-70b-versatile"
```

```bash
claudex profile test groq
```

<Aside type="tip">
  O Groq oferece inferencia extremamente rapida para modelos open-source. Modelos populares incluem `llama-3.3-70b-versatile`, `mixtral-8x7b-32768` e `gemma2-9b-it`.
</Aside>

### Mistral AI

1. Cadastre-se em [console.mistral.ai](https://console.mistral.ai/)
2. Crie uma chave API no console

```toml
[[profiles]]
name = "mistral"
provider_type = "OpenAICompatible"
base_url = "https://api.mistral.ai/v1"
api_key = "..."
default_model = "mistral-large-latest"
```

```bash
claudex profile test mistral
```

### Together AI

1. Cadastre-se em [api.together.ai](https://api.together.ai/)
2. Crie uma chave API no dashboard

```toml
[[profiles]]
name = "together"
provider_type = "OpenAICompatible"
base_url = "https://api.together.xyz/v1"
api_key = "..."
default_model = "meta-llama/Llama-3.3-70B-Instruct-Turbo"
```

```bash
claudex profile test together
```

### Perplexity

1. Cadastre-se em [perplexity.ai](https://www.perplexity.ai/)
2. Crie uma chave API em [perplexity.ai/settings/api](https://www.perplexity.ai/settings/api)

```toml
[[profiles]]
name = "perplexity"
provider_type = "OpenAICompatible"
base_url = "https://api.perplexity.ai"
api_key = "pplx-..."
default_model = "sonar-pro"
```

```bash
claudex profile test perplexity
```

<Aside type="note">
  Os modelos do Perplexity possuem capacidades de busca na web integradas. Util para tarefas orientadas a pesquisa.
</Aside>

### Cerebras

1. Cadastre-se em [cloud.cerebras.ai](https://cloud.cerebras.ai/)
2. Crie uma chave API no dashboard

```toml
[[profiles]]
name = "cerebras"
provider_type = "OpenAICompatible"
base_url = "https://api.cerebras.ai/v1"
api_key = "..."
default_model = "llama-3.3-70b"
```

```bash
claudex profile test cerebras
```

## Plataformas Cloud

### Azure OpenAI

1. Crie um recurso Azure OpenAI no [Portal Azure](https://portal.azure.com/)
2. Faca deploy de um modelo e anote o nome do recurso e do deployment
3. Obtenha sua chave API em **Keys and Endpoint**

```toml
[[profiles]]
name = "azure-openai"
provider_type = "OpenAICompatible"
base_url = "https://YOUR_RESOURCE.openai.azure.com/openai/deployments/YOUR_DEPLOYMENT"
api_key = "YOUR_AZURE_KEY"
default_model = "gpt-4o"

[profiles.query_params]
api-version = "2024-12-01-preview"
```

```bash
claudex profile test azure-openai
```

<Aside type="note">
  O Azure OpenAI e detectado automaticamente quando a `base_url` contem `openai.azure.com`. A autenticacao usa o header `api-key` em vez de `Authorization: Bearer`. O parametro de query `api-version` e obrigatorio.
</Aside>

### Google Vertex AI

1. Habilite a API Vertex AI no seu projeto GCP
2. Gere um token de acesso com `gcloud auth print-access-token`

```toml
[[profiles]]
name = "vertex-ai"
provider_type = "DirectAnthropic"
base_url = "https://us-east5-aiplatform.googleapis.com/v1/projects/YOUR_PROJECT/locations/us-east5/publishers/anthropic/models"
api_key = "YOUR_GCLOUD_TOKEN"
default_model = "claude-sonnet-4@20250514"
```

```bash
claudex profile test vertex-ai
```

<Aside type="caution">
  A saida do `gcloud auth print-access-token` expira apos ~1 hora. Para uso em producao, considere uma conta de servico com automacao de refresh de token.
</Aside>

### AWS Bedrock (via LiteLLM)

O AWS Bedrock e suportado atraves de um proxy LiteLLM:

1. Instale o LiteLLM: `pip install litellm`
2. Inicie o proxy:

```bash
litellm --model bedrock/anthropic.claude-sonnet-4-20250514-v2:0
```

3. Configure o Claudex:

```toml
[[profiles]]
name = "bedrock"
provider_type = "OpenAICompatible"
base_url = "http://localhost:4000/v1"
api_key = "sk-litellm"
default_model = "bedrock/anthropic.claude-sonnet-4-20250514-v2:0"
```

## Assinaturas OAuth

A autenticacao OAuth permite usar assinaturas existentes de provedores (ChatGPT Plus, Claude Max, etc.) em vez de chaves API separadas.

### Claude Max / Pro

Use sua assinatura Claude existente atraves da sessao OAuth nativa do Claude Code.

**Pre-requisitos**: Claude Code instalado e logado (o comando `claude` funciona normalmente)

```toml
[[profiles]]
name = "claude-max"
provider_type = "DirectAnthropic"
base_url = "https://api.claude.ai"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "claude"

[profiles.models]
haiku = "claude-haiku-4-20250514"
sonnet = "claude-sonnet-4-20250514"
opus = "claude-opus-4-20250514"
```

<Aside type="note">
  Profiles OAuth Claude usam um modo especial de gateway auth. O Claudex pula o proxy e deixa o Claude Code usar seu proprio OAuth diretamente. Nenhum passo separado de `claudex auth login` e necessario.
</Aside>

### ChatGPT Plus / Codex CLI

Use sua assinatura ChatGPT Plus ou Pro via token do Codex CLI.

**Pre-requisitos**: Instale o [Codex CLI](https://github.com/openai/codex) e autentique:

```bash
# Instalar Codex CLI
npm install -g @openai/codex

# Autenticar (abre o navegador)
codex auth
```

Em seguida, configure o Claudex:

```toml
[[profiles]]
name = "codex-sub"
provider_type = "OpenAIResponses"
base_url = "https://chatgpt.com/backend-api/codex"
default_model = "gpt-5.5"
auth_type = "oauth"
oauth_provider = "openai"

[profiles.models]
haiku = "gpt-5.5"
sonnet = "gpt-5.5"
opus = "gpt-5.5"
```

```bash
# Ler token do Codex CLI
claudex auth login openai --profile codex-sub

# Verificar
claudex auth status

# Executar
claudex run codex-sub
```

<Aside type="tip">
  Modelos suportados no endpoint Codex: `gpt-5.5`. Note que `gpt-4o` NAO e suportado no endpoint Codex do `chatgpt.com`. `strip_params = "auto"` detectara automaticamente este endpoint e removera `temperature`/`top_p`/`top_k`.
</Aside>

### Google Gemini

Use sua conta Google via credenciais do Gemini CLI.

```toml
[[profiles]]
name = "gemini-sub"
provider_type = "OpenAICompatible"
base_url = "https://generativelanguage.googleapis.com/v1beta/openai"
default_model = "gemini-2.5-pro"
auth_type = "oauth"
oauth_provider = "google"

[profiles.models]
haiku = "gemini-2.0-flash"
sonnet = "gemini-2.5-pro"
opus = "gemini-2.5-pro"
```

```bash
# Login le das credenciais do Gemini CLI
claudex auth login google --profile gemini-sub

# Verificar
claudex auth status
```

### Qwen / Tongyi Qianwen

Use sua conta Qwen via fluxo OAuth Device Code.

```toml
[[profiles]]
name = "qwen-oauth"
provider_type = "OpenAICompatible"
base_url = "https://chat.qwen.ai/api"
default_model = "qwen3-235b-a22b"
auth_type = "oauth"
oauth_provider = "qwen"
```

```bash
# Iniciar fluxo device code
claudex auth login qwen --profile qwen-oauth

# Verificar
claudex auth status
```

### Kimi / Moonshot (OAuth)

Use sua conta Kimi via credenciais do Kimi CLI.

```toml
[[profiles]]
name = "kimi-oauth"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.cn/v1"
default_model = "moonshot-v1-128k"
auth_type = "oauth"
oauth_provider = "kimi"
```

```bash
# Login le das credenciais do Kimi CLI
claudex auth login kimi --profile kimi-oauth

# Verificar
claudex auth status
```

### GitHub Copilot

Use sua assinatura GitHub Copilot via fluxo OAuth Device Code.

```toml
[[profiles]]
name = "copilot"
provider_type = "OpenAICompatible"
base_url = "https://api.githubcopilot.com"
default_model = "gpt-4o"
auth_type = "oauth"
oauth_provider = "github"
```

```bash
# Iniciar fluxo device code (abre o navegador para login no GitHub)
claudex auth login github --profile copilot

# Verificar
claudex auth status
```

### GitLab Duo

Use sua assinatura GitLab Duo via Personal Access Token.

```toml
[[profiles]]
name = "gitlab-duo"
provider_type = "OpenAICompatible"
base_url = "https://gitlab.com/api/v4/ai/llm/proxy"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "gitlab"
```

```bash
# Defina seu token GitLab como variavel de ambiente
export GITLAB_TOKEN=glpat-...

# Login
claudex auth login gitlab --profile gitlab-duo

# Verificar
claudex auth status
```

<Aside type="note">
  O GitLab Duo requer uma variavel de ambiente `GITLAB_TOKEN` com um Personal Access Token que tenha o escopo `ai_features`. Para instancias GitLab auto-hospedadas, atualize a `base_url` conforme necessario.
</Aside>

## Modelos Locais

### Ollama

1. Instale o Ollama: [ollama.com](https://ollama.com/)
2. Baixe um modelo:

```bash
ollama pull qwen2.5:72b
```

3. Configure o Claudex:

```toml
[[profiles]]
name = "local-qwen"
provider_type = "OpenAICompatible"
base_url = "http://localhost:11434/v1"
api_key = ""
default_model = "qwen2.5:72b"
```

```bash
claudex profile test local-qwen
```

<Aside type="note">
  O Ollama nao requer chave API. Deixe `api_key` vazio ou omita.
</Aside>

### vLLM

1. Instale e inicie o vLLM:

```bash
pip install vllm
vllm serve meta-llama/Llama-3.3-70B-Instruct --port 8000
```

2. Configure o Claudex:

```toml
[[profiles]]
name = "local-llama"
provider_type = "OpenAICompatible"
base_url = "http://localhost:8000/v1"
api_key = ""
default_model = "meta-llama/Llama-3.3-70B-Instruct"
```

### LM Studio

1. Baixe o LM Studio em [lmstudio.ai](https://lmstudio.ai/)
2. Carregue um modelo e inicie o servidor local (porta padrao: 1234)

```toml
[[profiles]]
name = "lm-studio"
provider_type = "OpenAICompatible"
base_url = "http://localhost:1234/v1"
api_key = "lm-studio"
default_model = "local-model"
```

## Verificando sua Configuracao

Apos configurar qualquer provedor, verifique a conectividade:

```bash
# Testar um profile especifico
claudex profile test <profile-name>

# Testar todos os profiles
claudex profile test all

# Listar todos os profiles configurados
claudex profile list
```
