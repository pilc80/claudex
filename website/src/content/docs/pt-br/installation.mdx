---
title: Instalacao
description: Como instalar o Claudex no seu sistema
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

## Metodos de Instalacao

<Tabs>
  <TabItem label="One-liner" icon="terminal">
    ```bash
    curl -fsSL https://raw.githubusercontent.com/pilc80/claudex/main/install.sh | bash
    ```
    O script de instalacao detecta automaticamente seu SO e arquitetura, baixa a versao mais recente do GitHub e instala em `~/.local/bin/claudex`.

    Para alterar o diretorio de instalacao:
    ```bash
    CLAUDEX_INSTALL_DIR=/usr/local/bin curl -fsSL https://raw.githubusercontent.com/pilc80/claudex/main/install.sh | bash
    ```
  </TabItem>
  <TabItem label="Cargo" icon="seti:rust">
    ```bash
    cargo install --git https://github.com/pilc80/claudex
    ```
    Requer o toolchain Rust. O binario sera instalado em `~/.cargo/bin/claudex`.
  </TabItem>
  <TabItem label="GitHub Releases" icon="github">
    Baixe o binario pre-compilado para sua plataforma em [GitHub Releases](https://github.com/pilc80/claudex/releases).

    Targets disponiveis:
    - `x86_64-unknown-linux-gnu`
    - `x86_64-unknown-linux-musl`
    - `aarch64-unknown-linux-gnu`
    - `aarch64-unknown-linux-musl`
    - `x86_64-apple-darwin`
    - `aarch64-apple-darwin`
    - `x86_64-pc-windows-msvc`

    Extraia e coloque o binario no seu `PATH`:
    ```bash
    tar xzf claudex-v*.tar.gz
    chmod +x claudex
    mv claudex ~/.local/bin/
    ```
  </TabItem>
</Tabs>

## Requisitos do Sistema

| Requisito | Detalhes |
|-----------|---------|
| **SO** | macOS (Intel / Apple Silicon), Linux (x86_64 / ARM64), Windows (x86_64) |
| **Claude Code** | Deve estar instalado e acessivel como `claude` no seu PATH |
| **Opcional** | [Ollama](https://ollama.com) para funcionalidades de roteamento inteligente e motor de contexto |

## Verificar a Instalacao

```bash
claudex-config --version
```

## Proximos Passos

Apos a instalacao, [configure seu primeiro profile de provedor](/pt-br/configuration/).
