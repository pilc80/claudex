---
title: Руководство по настройке провайдеров
description: Пошаговые инструкции по настройке каждого AI-провайдера в Claudex
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

Это руководство содержит подробные инструкции по настройке всех провайдеров, поддерживаемых Claudex, включая провайдеров с API-ключами, OAuth-подписки, облачные платформы и локальные модели.

## Провайдеры с API-ключами

### Anthropic

1. Зарегистрируйтесь на [console.anthropic.com](https://console.anthropic.com/)
2. Перейдите в **API Keys** и создайте новый ключ (формат: `sk-ant-*`)

```toml
[[profiles]]
name = "anthropic"
provider_type = "DirectAnthropic"
base_url = "https://api.anthropic.com"
api_key = "sk-ant-..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex-config profile test anthropic
```

### MiniMax

1. Зарегистрируйтесь на [platform.minimaxi.com](https://platform.minimaxi.com/)
2. Перейдите в **API Keys** в консоли

```toml
[[profiles]]
name = "minimax"
provider_type = "DirectAnthropic"
base_url = "https://api.minimax.io/anthropic"
api_key = "..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex-config profile test minimax
```

### OpenRouter

1. Зарегистрируйтесь на [openrouter.ai](https://openrouter.ai/)
2. Получите API-ключ на [openrouter.ai/keys](https://openrouter.ai/keys)

```toml
[[profiles]]
name = "openrouter"
provider_type = "OpenAICompatible"
base_url = "https://openrouter.ai/api/v1"
api_key = "sk-or-..."
default_model = "anthropic/claude-sonnet-4"
```

```bash
claudex-config profile test openrouter
```

<Aside type="tip">
  OpenRouter поддерживает сотни моделей от разных провайдеров. Используйте префикс `anthropic/` для моделей Claude.
</Aside>

### Grok (xAI)

1. Зарегистрируйтесь на [console.x.ai](https://console.x.ai/)
2. Создайте API-ключ в консоли

```toml
[[profiles]]
name = "grok"
provider_type = "OpenAICompatible"
base_url = "https://api.x.ai/v1"
api_key = "xai-..."
default_model = "grok-3-beta"
```

```bash
claudex-config profile test grok
```

### OpenAI

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com/)
2. Создайте API-ключ на [platform.openai.com/api-keys](https://platform.openai.com/api-keys)

```toml
[[profiles]]
name = "chatgpt"
provider_type = "OpenAICompatible"
base_url = "https://api.openai.com/v1"
api_key = "sk-..."
default_model = "gpt-4o"
```

```bash
claudex-config profile test chatgpt
```

### DeepSeek

1. Зарегистрируйтесь на [platform.deepseek.com](https://platform.deepseek.com/)
2. Создайте API-ключ в консоли

```toml
[[profiles]]
name = "deepseek"
provider_type = "OpenAICompatible"
base_url = "https://api.deepseek.com"
api_key = "sk-..."
default_model = "deepseek-chat"
```

```bash
claudex-config profile test deepseek
```

### Kimi / Moonshot

1. Зарегистрируйтесь на [platform.moonshot.cn](https://platform.moonshot.cn/)
2. Создайте API-ключ в консоли

```toml
[[profiles]]
name = "kimi"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.ai/v1"
api_key = "sk-..."
default_model = "kimi-k2-0905-preview"
```

```bash
claudex-config profile test kimi
```

### GLM / Zhipu

1. Зарегистрируйтесь на [open.bigmodel.cn](https://open.bigmodel.cn/)
2. Создайте API-ключ в консоли

```toml
[[profiles]]
name = "glm"
provider_type = "OpenAICompatible"
base_url = "https://api.z.ai/api/paas/v4"
api_key = "..."
default_model = "glm-4.6"
```

```bash
claudex-config profile test glm
```

### Groq

1. Зарегистрируйтесь на [console.groq.com](https://console.groq.com/)
2. Создайте API-ключ в **Settings > API Keys**

```toml
[[profiles]]
name = "groq"
provider_type = "OpenAICompatible"
base_url = "https://api.groq.com/openai/v1"
api_key = "gsk_..."
default_model = "llama-3.3-70b-versatile"
```

```bash
claudex-config profile test groq
```

<Aside type="tip">
  Groq обеспечивает крайне быстрый инференс для моделей с открытым кодом. Популярные модели: `llama-3.3-70b-versatile`, `mixtral-8x7b-32768` и `gemma2-9b-it`.
</Aside>

### Mistral AI

1. Зарегистрируйтесь на [console.mistral.ai](https://console.mistral.ai/)
2. Создайте API-ключ в консоли

```toml
[[profiles]]
name = "mistral"
provider_type = "OpenAICompatible"
base_url = "https://api.mistral.ai/v1"
api_key = "..."
default_model = "mistral-large-latest"
```

```bash
claudex-config profile test mistral
```

### Together AI

1. Зарегистрируйтесь на [api.together.ai](https://api.together.ai/)
2. Создайте API-ключ в панели управления

```toml
[[profiles]]
name = "together"
provider_type = "OpenAICompatible"
base_url = "https://api.together.xyz/v1"
api_key = "..."
default_model = "meta-llama/Llama-3.3-70B-Instruct-Turbo"
```

```bash
claudex-config profile test together
```

### Perplexity

1. Зарегистрируйтесь на [perplexity.ai](https://www.perplexity.ai/)
2. Создайте API-ключ на [perplexity.ai/settings/api](https://www.perplexity.ai/settings/api)

```toml
[[profiles]]
name = "perplexity"
provider_type = "OpenAICompatible"
base_url = "https://api.perplexity.ai"
api_key = "pplx-..."
default_model = "sonar-pro"
```

```bash
claudex-config profile test perplexity
```

<Aside type="note">
  Модели Perplexity имеют встроенные возможности веб-поиска. Полезно для исследовательских задач.
</Aside>

### Cerebras

1. Зарегистрируйтесь на [cloud.cerebras.ai](https://cloud.cerebras.ai/)
2. Создайте API-ключ в панели управления

```toml
[[profiles]]
name = "cerebras"
provider_type = "OpenAICompatible"
base_url = "https://api.cerebras.ai/v1"
api_key = "..."
default_model = "llama-3.3-70b"
```

```bash
claudex-config profile test cerebras
```

## Облачные платформы

### Azure OpenAI

1. Создайте ресурс Azure OpenAI в [Azure Portal](https://portal.azure.com/)
2. Разверните модель и запишите имя ресурса и имя развёртывания
3. Получите API-ключ в разделе **Keys and Endpoint**

```toml
[[profiles]]
name = "azure-openai"
provider_type = "OpenAICompatible"
base_url = "https://YOUR_RESOURCE.openai.azure.com/openai/deployments/YOUR_DEPLOYMENT"
api_key = "YOUR_AZURE_KEY"
default_model = "gpt-4o"

[profiles.query_params]
api-version = "2024-12-01-preview"
```

```bash
claudex-config profile test azure-openai
```

<Aside type="note">
  Azure OpenAI автоматически определяется по `base_url`, содержащему `openai.azure.com`. Аутентификация использует заголовок `api-key` вместо `Authorization: Bearer`. Query-параметр `api-version` обязателен.
</Aside>

### Google Vertex AI

1. Включите API Vertex AI в вашем GCP-проекте
2. Сгенерируйте токен доступа с помощью `gcloud auth print-access-token`

```toml
[[profiles]]
name = "vertex-ai"
provider_type = "DirectAnthropic"
base_url = "https://us-east5-aiplatform.googleapis.com/v1/projects/YOUR_PROJECT/locations/us-east5/publishers/anthropic/models"
api_key = "YOUR_GCLOUD_TOKEN"
default_model = "claude-sonnet-4@20250514"
```

```bash
claudex-config profile test vertex-ai
```

<Aside type="caution">
  Вывод `gcloud auth print-access-token` истекает примерно через 1 час. Для промышленного использования рассмотрите сервисный аккаунт с автоматическим обновлением токена.
</Aside>

### AWS Bedrock (через LiteLLM)

AWS Bedrock поддерживается через прокси LiteLLM:

1. Установите LiteLLM: `pip install litellm`
2. Запустите прокси:

```bash
litellm --model bedrock/anthropic.claude-sonnet-4-20250514-v2:0
```

3. Настройте Claudex:

```toml
[[profiles]]
name = "bedrock"
provider_type = "OpenAICompatible"
base_url = "http://localhost:4000/v1"
api_key = "sk-litellm"
default_model = "bedrock/anthropic.claude-sonnet-4-20250514-v2:0"
```

## OAuth-подписки

OAuth-аутентификация позволяет использовать существующие подписки провайдеров (ChatGPT Plus, Claude Max и т.д.) вместо отдельных API-ключей.

### Claude Max / Pro

Используйте существующую подписку Claude через нативную OAuth-сессию Claude Code.

**Предварительные условия**: Claude Code установлен и выполнен вход (команда `claude` работает нормально)

```toml
[[profiles]]
name = "claude-max"
provider_type = "DirectAnthropic"
base_url = "https://api.claude.ai"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "claude"

[profiles.models]
haiku = "claude-haiku-4-20250514"
sonnet = "claude-sonnet-4-20250514"
opus = "claude-opus-4-20250514"
```

<Aside type="note">
  OAuth-профили Claude используют специальный режим gateway auth. Claudex пропускает прокси и позволяет Claude Code использовать собственный OAuth напрямую. Отдельный шаг `claudex-config auth login` не требуется.
</Aside>

### ChatGPT Plus / Codex CLI

Используйте подписку ChatGPT Plus или Pro через токен Codex CLI.

**Предварительные условия**: Установите [Codex CLI](https://github.com/openai/codex) и выполните аутентификацию:

```bash
# Установить Codex CLI
npm install -g @openai/codex

# Аутентифицироваться (открывает браузер)
codex auth
```

Затем настройте Claudex:

```toml
[[profiles]]
name = "codex-sub"
provider_type = "OpenAIResponses"
base_url = "https://chatgpt.com/backend-api/codex"
default_model = "gpt-5.5"
auth_type = "oauth"
oauth_provider = "openai"

[profiles.models]
haiku = "gpt-5.5"
sonnet = "gpt-5.5"
opus = "gpt-5.5"
```

```bash
# Считать токен из Codex CLI
claudex-config auth login openai --profile codex-sub

# Проверить
claudex-config auth status

# Запустить
CLAUDEX_PROFILE=codex-sub claudex
```

<Aside type="tip">
  Поддерживаемые модели на Codex endpoint: `gpt-5.5`. Модель `gpt-4o` НЕ поддерживается на endpoint `chatgpt.com` Codex. `strip_params = "auto"` автоматически обнаруживает этот endpoint и удаляет `temperature`/`top_p`/`top_k`.
</Aside>

### Google Gemini

Используйте аккаунт Google через учётные данные Gemini CLI.

```toml
[[profiles]]
name = "gemini-sub"
provider_type = "OpenAICompatible"
base_url = "https://generativelanguage.googleapis.com/v1beta/openai"
default_model = "gemini-2.5-pro"
auth_type = "oauth"
oauth_provider = "google"

[profiles.models]
haiku = "gemini-2.0-flash"
sonnet = "gemini-2.5-pro"
opus = "gemini-2.5-pro"
```

```bash
# Логин считывает из учётных данных Gemini CLI
claudex-config auth login google --profile gemini-sub

# Проверить
claudex-config auth status
```

### Qwen / Tongyi Qianwen

Используйте аккаунт Qwen через OAuth Device Code flow.

```toml
[[profiles]]
name = "qwen-oauth"
provider_type = "OpenAICompatible"
base_url = "https://chat.qwen.ai/api"
default_model = "qwen3-235b-a22b"
auth_type = "oauth"
oauth_provider = "qwen"
```

```bash
# Запустить device code flow
claudex-config auth login qwen --profile qwen-oauth

# Проверить
claudex-config auth status
```

### Kimi / Moonshot (OAuth)

Используйте аккаунт Kimi через учётные данные Kimi CLI.

```toml
[[profiles]]
name = "kimi-oauth"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.cn/v1"
default_model = "moonshot-v1-128k"
auth_type = "oauth"
oauth_provider = "kimi"
```

```bash
# Логин считывает из учётных данных Kimi CLI
claudex-config auth login kimi --profile kimi-oauth

# Проверить
claudex-config auth status
```

### GitHub Copilot

Используйте подписку GitHub Copilot через OAuth Device Code flow.

```toml
[[profiles]]
name = "copilot"
provider_type = "OpenAICompatible"
base_url = "https://api.githubcopilot.com"
default_model = "gpt-4o"
auth_type = "oauth"
oauth_provider = "github"
```

```bash
# Запустить device code flow (открывает браузер для входа через GitHub)
claudex-config auth login github --profile copilot

# Проверить
claudex-config auth status
```

### GitLab Duo

Используйте подписку GitLab Duo через Personal Access Token.

```toml
[[profiles]]
name = "gitlab-duo"
provider_type = "OpenAICompatible"
base_url = "https://gitlab.com/api/v4/ai/llm/proxy"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "gitlab"
```

```bash
# Установите токен GitLab как переменную окружения
export GITLAB_TOKEN=glpat-...

# Логин
claudex-config auth login gitlab --profile gitlab-duo

# Проверить
claudex-config auth status
```

<Aside type="note">
  GitLab Duo требует переменную окружения `GITLAB_TOKEN` с Personal Access Token, имеющим scope `ai_features`. Для self-managed инстансов GitLab обновите `base_url` соответственно.
</Aside>

## Локальные модели

### Ollama

1. Установите Ollama: [ollama.com](https://ollama.com/)
2. Загрузите модель:

```bash
ollama pull qwen2.5:72b
```

3. Настройте Claudex:

```toml
[[profiles]]
name = "local-qwen"
provider_type = "OpenAICompatible"
base_url = "http://localhost:11434/v1"
api_key = ""
default_model = "qwen2.5:72b"
```

```bash
claudex-config profile test local-qwen
```

<Aside type="note">
  Ollama не требует API-ключа. Оставьте `api_key` пустым или не указывайте его.
</Aside>

### vLLM

1. Установите и запустите vLLM:

```bash
pip install vllm
vllm serve meta-llama/Llama-3.3-70B-Instruct --port 8000
```

2. Настройте Claudex:

```toml
[[profiles]]
name = "local-llama"
provider_type = "OpenAICompatible"
base_url = "http://localhost:8000/v1"
api_key = ""
default_model = "meta-llama/Llama-3.3-70B-Instruct"
```

### LM Studio

1. Скачайте LM Studio с [lmstudio.ai](https://lmstudio.ai/)
2. Загрузите модель и запустите локальный сервер (порт по умолчанию: 1234)

```toml
[[profiles]]
name = "lm-studio"
provider_type = "OpenAICompatible"
base_url = "http://localhost:1234/v1"
api_key = "lm-studio"
default_model = "local-model"
```

## Проверка настройки

После настройки любого провайдера проверьте подключение:

```bash
# Тестировать конкретный профиль
claudex-config profile test <profile-name>

# Тестировать все профили
claudex-config profile test all

# Список всех настроенных профилей
claudex-config profile list
```
