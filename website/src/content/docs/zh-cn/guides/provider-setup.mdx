---
title: 提供商设置指南
description: 每个 AI 提供商的详细配置步骤
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

本指南详细介绍 Claudex 支持的每个提供商的配置方法，包括 API Key 提供商、OAuth 订阅、云平台和本地模型。

## API Key 提供商

### Anthropic

1. 在 [console.anthropic.com](https://console.anthropic.com/) 注册
2. 进入 **API Keys** 页面创建密钥（格式：`sk-ant-*`）

```toml
[[profiles]]
name = "anthropic"
provider_type = "DirectAnthropic"
base_url = "https://api.anthropic.com"
api_key = "sk-ant-..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex-config profile test anthropic
```

### MiniMax

1. 在 [platform.minimaxi.com](https://platform.minimaxi.com/) 注册
2. 在控制台中进入 **API Keys** 页面

```toml
[[profiles]]
name = "minimax"
provider_type = "DirectAnthropic"
base_url = "https://api.minimax.io/anthropic"
api_key = "..."
default_model = "claude-sonnet-4-20250514"
```

```bash
claudex-config profile test minimax
```

### OpenRouter

1. 在 [openrouter.ai](https://openrouter.ai/) 注册
2. 在 [openrouter.ai/keys](https://openrouter.ai/keys) 获取 API 密钥

```toml
[[profiles]]
name = "openrouter"
provider_type = "OpenAICompatible"
base_url = "https://openrouter.ai/api/v1"
api_key = "sk-or-..."
default_model = "anthropic/claude-sonnet-4"
```

```bash
claudex-config profile test openrouter
```

<Aside type="tip">
  OpenRouter 支持来自不同提供商的数百个模型。Claude 模型使用 `anthropic/` 前缀。
</Aside>

### Grok (xAI)

1. 在 [console.x.ai](https://console.x.ai/) 注册
2. 在控制台中创建 API 密钥

```toml
[[profiles]]
name = "grok"
provider_type = "OpenAICompatible"
base_url = "https://api.x.ai/v1"
api_key = "xai-..."
default_model = "grok-3-beta"
```

```bash
claudex-config profile test grok
```

### OpenAI

1. 在 [platform.openai.com](https://platform.openai.com/) 注册
2. 在 [platform.openai.com/api-keys](https://platform.openai.com/api-keys) 创建 API 密钥

```toml
[[profiles]]
name = "chatgpt"
provider_type = "OpenAICompatible"
base_url = "https://api.openai.com/v1"
api_key = "sk-..."
default_model = "gpt-4o"
```

```bash
claudex-config profile test chatgpt
```

### DeepSeek

1. 在 [platform.deepseek.com](https://platform.deepseek.com/) 注册
2. 在控制台中创建 API 密钥

```toml
[[profiles]]
name = "deepseek"
provider_type = "OpenAICompatible"
base_url = "https://api.deepseek.com"
api_key = "sk-..."
default_model = "deepseek-chat"
```

```bash
claudex-config profile test deepseek
```

### Kimi / Moonshot

1. 在 [platform.moonshot.cn](https://platform.moonshot.cn/) 注册
2. 在控制台中创建 API 密钥

```toml
[[profiles]]
name = "kimi"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.ai/v1"
api_key = "sk-..."
default_model = "kimi-k2-0905-preview"
```

```bash
claudex-config profile test kimi
```

### GLM / 智谱

1. 在 [open.bigmodel.cn](https://open.bigmodel.cn/) 注册
2. 在控制台中创建 API 密钥

```toml
[[profiles]]
name = "glm"
provider_type = "OpenAICompatible"
base_url = "https://api.z.ai/api/paas/v4"
api_key = "..."
default_model = "glm-4.6"
```

```bash
claudex-config profile test glm
```

### Groq

1. 在 [console.groq.com](https://console.groq.com/) 注册
2. 在 **Settings > API Keys** 中创建 API 密钥

```toml
[[profiles]]
name = "groq"
provider_type = "OpenAICompatible"
base_url = "https://api.groq.com/openai/v1"
api_key = "gsk_..."
default_model = "llama-3.3-70b-versatile"
```

```bash
claudex-config profile test groq
```

<Aside type="tip">
  Groq 为开源模型提供极快的推理速度。常用模型包括 `llama-3.3-70b-versatile`、`mixtral-8x7b-32768` 和 `gemma2-9b-it`。
</Aside>

### Mistral AI

1. 在 [console.mistral.ai](https://console.mistral.ai/) 注册
2. 在控制台中创建 API 密钥

```toml
[[profiles]]
name = "mistral"
provider_type = "OpenAICompatible"
base_url = "https://api.mistral.ai/v1"
api_key = "..."
default_model = "mistral-large-latest"
```

```bash
claudex-config profile test mistral
```

### Together AI

1. 在 [api.together.ai](https://api.together.ai/) 注册
2. 在仪表盘中创建 API 密钥

```toml
[[profiles]]
name = "together"
provider_type = "OpenAICompatible"
base_url = "https://api.together.xyz/v1"
api_key = "..."
default_model = "meta-llama/Llama-3.3-70B-Instruct-Turbo"
```

```bash
claudex-config profile test together
```

### Perplexity

1. 在 [perplexity.ai](https://www.perplexity.ai/) 注册
2. 在 [perplexity.ai/settings/api](https://www.perplexity.ai/settings/api) 创建 API 密钥

```toml
[[profiles]]
name = "perplexity"
provider_type = "OpenAICompatible"
base_url = "https://api.perplexity.ai"
api_key = "pplx-..."
default_model = "sonar-pro"
```

```bash
claudex-config profile test perplexity
```

<Aside type="note">
  Perplexity 模型内置网页搜索功能，适合研究导向的任务。
</Aside>

### Cerebras

1. 在 [cloud.cerebras.ai](https://cloud.cerebras.ai/) 注册
2. 在仪表盘中创建 API 密钥

```toml
[[profiles]]
name = "cerebras"
provider_type = "OpenAICompatible"
base_url = "https://api.cerebras.ai/v1"
api_key = "..."
default_model = "llama-3.3-70b"
```

```bash
claudex-config profile test cerebras
```

## 云平台

### Azure OpenAI

1. 在 [Azure Portal](https://portal.azure.com/) 创建 Azure OpenAI 资源
2. 部署模型并记录资源名称和部署名称
3. 从 **Keys and Endpoint** 获取 API 密钥

```toml
[[profiles]]
name = "azure-openai"
provider_type = "OpenAICompatible"
base_url = "https://YOUR_RESOURCE.openai.azure.com/openai/deployments/YOUR_DEPLOYMENT"
api_key = "YOUR_AZURE_KEY"
default_model = "gpt-4o"

[profiles.query_params]
api-version = "2024-12-01-preview"
```

```bash
claudex-config profile test azure-openai
```

<Aside type="note">
  Azure OpenAI 通过 `base_url` 包含 `openai.azure.com` 自动检测。认证使用 `api-key` 请求头（取代 `Authorization: Bearer`）。`api-version` 查询参数为必填。
</Aside>

### Google Vertex AI

1. 在 GCP 项目中启用 Vertex AI API
2. 使用 `gcloud auth print-access-token` 生成访问令牌

```toml
[[profiles]]
name = "vertex-ai"
provider_type = "DirectAnthropic"
base_url = "https://us-east5-aiplatform.googleapis.com/v1/projects/YOUR_PROJECT/locations/us-east5/publishers/anthropic/models"
api_key = "YOUR_GCLOUD_TOKEN"
default_model = "claude-sonnet-4@20250514"
```

```bash
claudex-config profile test vertex-ai
```

<Aside type="caution">
  `gcloud auth print-access-token` 输出的令牌约 1 小时后过期。生产环境建议使用带有自动令牌刷新的服务账号。
</Aside>

### AWS Bedrock（通过 LiteLLM）

AWS Bedrock 通过 LiteLLM 代理支持：

1. 安装 LiteLLM：`pip install litellm`
2. 启动代理：

```bash
litellm --model bedrock/anthropic.claude-sonnet-4-20250514-v2:0
```

3. 配置 Claudex：

```toml
[[profiles]]
name = "bedrock"
provider_type = "OpenAICompatible"
base_url = "http://localhost:4000/v1"
api_key = "sk-litellm"
default_model = "bedrock/anthropic.claude-sonnet-4-20250514-v2:0"
```

## OAuth 订阅认证

OAuth 认证允许你使用现有的提供商订阅（ChatGPT Plus、Claude Max 等），无需单独的 API 密钥。

### Claude Max / Pro

使用你现有的 Claude 订阅，通过 Claude Code 的原生 OAuth 会话。

**前置条件**：已安装 Claude Code 且已登录（`claude` 命令正常工作）

```toml
[[profiles]]
name = "claude-max"
provider_type = "DirectAnthropic"
base_url = "https://api.claude.ai"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "claude"

[profiles.models]
haiku = "claude-haiku-4-20250514"
sonnet = "claude-sonnet-4-20250514"
opus = "claude-opus-4-20250514"
```

<Aside type="note">
  Claude OAuth profile 使用特殊的网关认证模式。Claudex 跳过代理，让 Claude Code 直接使用自身的 OAuth。无需额外的 `claudex-config auth login` 步骤。
</Aside>

### ChatGPT Plus / Codex CLI

通过 Codex CLI token 使用你的 ChatGPT Plus 或 Pro 订阅。

**前置条件**：安装 [Codex CLI](https://github.com/openai/codex) 并完成认证：

```bash
# 安装 Codex CLI
npm install -g @openai/codex

# 认证（会打开浏览器）
codex auth
```

然后配置 Claudex：

```toml
[[profiles]]
name = "codex-sub"
provider_type = "OpenAIResponses"
base_url = "https://chatgpt.com/backend-api/codex"
default_model = "gpt-5.5"
auth_type = "oauth"
oauth_provider = "openai"

[profiles.models]
haiku = "gpt-5.5"
sonnet = "gpt-5.5"
opus = "gpt-5.5"
```

```bash
# 从 Codex CLI 读取 token
claudex-config auth login openai --profile codex-sub

# 验证
claudex-config auth status

# 运行
CLAUDEX_PROFILE=codex-sub claudex
```

<Aside type="tip">
  Codex 端点支持的模型：`gpt-5.5`、`gpt-5.5`、`gpt-5.5`。注意 `gpt-4o` 在 `chatgpt.com` Codex 端点上不受支持。`strip_params = "auto"` 会自动检测此端点并剥离 `temperature`/`top_p`/`top_k`。
</Aside>

### Google Gemini

通过 Gemini CLI 凭证使用你的 Google 账户。

```toml
[[profiles]]
name = "gemini-sub"
provider_type = "OpenAICompatible"
base_url = "https://generativelanguage.googleapis.com/v1beta/openai"
default_model = "gemini-2.5-pro"
auth_type = "oauth"
oauth_provider = "google"

[profiles.models]
haiku = "gemini-2.0-flash"
sonnet = "gemini-2.5-pro"
opus = "gemini-2.5-pro"
```

```bash
# 从 Gemini CLI 凭证读取登录信息
claudex-config auth login google --profile gemini-sub

# 验证
claudex-config auth status
```

### Qwen / 通义千问

通过 OAuth Device Code 流程使用你的 Qwen 账户。

```toml
[[profiles]]
name = "qwen-oauth"
provider_type = "OpenAICompatible"
base_url = "https://chat.qwen.ai/api"
default_model = "qwen3-235b-a22b"
auth_type = "oauth"
oauth_provider = "qwen"
```

```bash
# 启动 Device Code 流程
claudex-config auth login qwen --profile qwen-oauth

# 验证
claudex-config auth status
```

### Kimi / Moonshot (OAuth)

通过 Kimi CLI 凭证使用你的 Kimi 账户。

```toml
[[profiles]]
name = "kimi-oauth"
provider_type = "OpenAICompatible"
base_url = "https://api.moonshot.cn/v1"
default_model = "moonshot-v1-128k"
auth_type = "oauth"
oauth_provider = "kimi"
```

```bash
# 从 Kimi CLI 凭证读取登录信息
claudex-config auth login kimi --profile kimi-oauth

# 验证
claudex-config auth status
```

### GitHub Copilot

通过 OAuth Device Code 流程使用你的 GitHub Copilot 订阅。

```toml
[[profiles]]
name = "copilot"
provider_type = "OpenAICompatible"
base_url = "https://api.githubcopilot.com"
default_model = "gpt-4o"
auth_type = "oauth"
oauth_provider = "github"
```

```bash
# 启动 Device Code 流程（打开浏览器进行 GitHub 登录）
claudex-config auth login github --profile copilot

# 验证
claudex-config auth status
```

### GitLab Duo

通过个人访问令牌使用你的 GitLab Duo 订阅。

```toml
[[profiles]]
name = "gitlab-duo"
provider_type = "OpenAICompatible"
base_url = "https://gitlab.com/api/v4/ai/llm/proxy"
default_model = "claude-sonnet-4-20250514"
auth_type = "oauth"
oauth_provider = "gitlab"
```

```bash
# 将 GitLab token 设为环境变量
export GITLAB_TOKEN=glpat-...

# 登录
claudex-config auth login gitlab --profile gitlab-duo

# 验证
claudex-config auth status
```

<Aside type="note">
  GitLab Duo 需要带有 `ai_features` 权限范围的个人访问令牌，通过 `GITLAB_TOKEN` 环境变量提供。自托管 GitLab 实例请相应修改 `base_url`。
</Aside>

## 本地模型

### Ollama

1. 安装 Ollama：[ollama.com](https://ollama.com/)
2. 拉取模型：

```bash
ollama pull qwen2.5:72b
```

3. 配置 Claudex：

```toml
[[profiles]]
name = "local-qwen"
provider_type = "OpenAICompatible"
base_url = "http://localhost:11434/v1"
api_key = ""
default_model = "qwen2.5:72b"
```

```bash
claudex-config profile test local-qwen
```

<Aside type="note">
  Ollama 不需要 API 密钥。`api_key` 留空或省略即可。
</Aside>

### vLLM

1. 安装并启动 vLLM：

```bash
pip install vllm
vllm serve meta-llama/Llama-3.3-70B-Instruct --port 8000
```

2. 配置 Claudex：

```toml
[[profiles]]
name = "local-llama"
provider_type = "OpenAICompatible"
base_url = "http://localhost:8000/v1"
api_key = ""
default_model = "meta-llama/Llama-3.3-70B-Instruct"
```

### LM Studio

1. 从 [lmstudio.ai](https://lmstudio.ai/) 下载 LM Studio
2. 加载模型并启动本地服务器（默认端口：1234）

```toml
[[profiles]]
name = "lm-studio"
provider_type = "OpenAICompatible"
base_url = "http://localhost:1234/v1"
api_key = "lm-studio"
default_model = "local-model"
```

## 验证配置

配置任何提供商后，验证连通性：

```bash
# 测试指定 profile
claudex-config profile test <profile-name>

# 测试所有 profile
claudex-config profile test all

# 列出所有已配置的 profile
claudex-config profile list
```
